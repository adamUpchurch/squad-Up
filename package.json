{
  "name": "sqaud-up",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "homepage" : "http://squad-up.s3-website-us-east-1.amazonaws.com/",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "aws s3 sync build/ s3://squad-up",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
